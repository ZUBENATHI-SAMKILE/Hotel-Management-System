spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/hotel_db
    username: postgres
    password: 1111
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: group.bookbtore@gmail.com
    password: ckhe jwbw xcnx ovya
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        from: group.bookbtore@gmail.com

app:
  jwt:
    secret: hotel-management-zubenathi-secret-key-2025-very-long-string
    expiration: 86400000
  frontend-url: ${FRONTEND_URL:http://localhost:5173}

server:
  port: 8080

logging:
  level:
    com.zubenathi: DEBUG