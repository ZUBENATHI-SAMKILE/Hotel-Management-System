spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5434/hotel_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:1111}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:group.bookbtore@gmail.com}
    password: ${MAIL_PASSWORD:ckhe jwbw xcnx ovya}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        from: ${MAIL_USERNAME:group.bookbtore@gmail.com}

app:
  jwt:
    secret: ${JWT_SECRET:hotel-management-zubenathi-secret-key-2025-very-long-string}
    expiration: ${JWT_EXPIRATION:86400000}
  frontend-url: ${FRONTEND_URL:http://localhost:5173}

server:
  port: ${PORT:8080}

logging:
  level:
    com.zubenathi: ${LOG_LEVEL:INFO}